spring.application.name=Vasthra
server.port=8081
spring.datasource.url=JDBC:MYSQL://LOCALHOST:3306/Vastra_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=divya
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Let Hibernate create/update tables automatically
spring.jpa.hibernate.ddl-auto=update

# show SQL (dev only)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Use newer dialect if needed
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect